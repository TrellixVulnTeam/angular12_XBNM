import { HighlightEventDirective } from './highlight-event.directive';

describe('HighlightEventDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightEventDirective();
    expect(directive).toBeTruthy();
  });
});
